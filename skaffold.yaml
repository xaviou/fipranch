apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/xaviou/fipranch:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/xaviou/fipranch:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: fipranch
        chartPath: charts/fipranch
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/xaviou/fipranch'
          image.tag: '{{.DIGEST_HEX}}'
